"""
Pytest configuration for the dashboard project.
"""
import os
import django

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'config.settings.base')


def pytest_configure(config):
    """Configure pytest for Django."""
    django.setup()
